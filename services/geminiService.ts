// This service is no longer used as AI features have been disabled.
// Keeping the file placeholder to avoid potential import errors if referenced elsewhere,
// but the content is removed to avoid dependency on @google/genai.
export {};
